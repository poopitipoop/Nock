[package]
name = "{{project_name}}"
version = {{toml_version}}
edition = "2021"
authors = {{toml_authors}}
description = {{toml_description}}

[[bin]]
name = "{{project_name}}"
path = "src/main.rs"

[dependencies]
# NockVM dependency
nockapp = { git = "https://github.com/nockchain/nockchain.git", rev = "{{nockchain_rev}}" }
nockvm = { git = "https://github.com/nockchain/nockchain.git", rev = "{{nockchain_rev}}" }
nockvm_macros = { git = "https://github.com/nockchain/nockchain.git", rev = "{{nockchain_rev}}" }

# Common dependencies for NockApps
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
anyhow = "1.0"
bytes = "1.5.0"
tokio = { version = "1.32", features = [
  "fs",
  "io-util",
  "macros",
  "net",
  "rt-multi-thread",
  "rt",
  "signal",
] }
tokio-util = "0.7.11"

[build-dependencies]
# Build script dependencies if needed
